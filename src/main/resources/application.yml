spring:
  datasource:
    driverClassName: org.h2.Driver
  h2.console:
    enabled: true
    path: '/console'
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: validate